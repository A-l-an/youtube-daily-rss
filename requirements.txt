feedparser>=6.0.11
# curl_cffi powers yt-dlp's --impersonate (browser-TLS) to pass YouTube's bot gate.
# Requires a non-anaconda Python venv (anaconda's OpenSSL clashes with its BoringSSL).
curl_cffi>=0.7.0
httpx[socks]>=0.28.1
openai>=1.70.0
pydub>=0.25.1
python-dotenv>=1.0.1
PyYAML>=6.0.2
requests>=2.32.0
webvtt-py>=0.5.1
youtube-transcript-api>=1.2.0
yt-dlp>=2024.8.6
